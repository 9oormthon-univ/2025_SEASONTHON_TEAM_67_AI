name: deploy-fastapi
on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sync code to C:\apps\py (mirror)
        shell: powershell
        run: |
          Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
          $src = "$env:GITHUB_WORKSPACE"
          $dst = "C:\apps\py"
          robocopy $src $dst /MIR /XD .git .github venv /XF .env py-out.log py-err.log /NDL /NFL
          if ($LASTEXITCODE -ge 8) { exit $LASTEXITCODE } else { exit 0 }

      - name: Write .env from secret
        shell: powershell
        run: |
          $envPath = "C:\apps\py\.env"
          @'
          ${{ secrets.FASTAPI_ENV_FILE }}
          '@ | Set-Content -Path $envPath

      - name: Install Python deps (venv already exists)
        shell: powershell
        run: |
          Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
          $venvPy = "C:\apps\py\venv\Scripts\python.exe"
          # pip 강제 설치 (ensurepip)
          & $venvPy -m ensurepip --upgrade
          # pip 자체를 업그레이드
          & $venvPy -m pip install --upgrade pip
          # requirements 설치
          & $venvPy -m pip install -r C:\apps\py\requirements.txt

      - name: Restart py-app (NSSM)
        shell: powershell
        run: |
          Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
          C:\nssm\nssm.exe restart py-app
